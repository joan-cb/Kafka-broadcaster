apiVersion: v1
kind: Service
metadata:
  name: kafka-broadcaster
  namespace: kafka-broadcaster
  labels:
    app.kubernetes.io/name: kafka-broadcaster
    app.kubernetes.io/instance: kafka-broadcaster
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
      protocol: TCP
  selector:
    app.kubernetes.io/name: kafka-broadcaster
    app.kubernetes.io/instance: kafka-broadcaster
